# Suggested environment for the assessment — optional, use whatever you like.
# Tested with Python 3.11.

# data + modelling
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
# a gradient-boosting library tends to do well on tabular fraud data; pick one:
# lightgbm>=4.0
# xgboost>=2.0

# serving (Part B) — pick whatever you're comfortable with
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# dashboard (Part C) — one option; a small React/HTML page is equally fine
streamlit>=1.30

# plotting (optional)
matplotlib>=3.7
